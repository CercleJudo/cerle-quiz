services:
  - type: web
    name: cerle-quiz
    env: python
    branch: main
    buildCommand: pip install -r requirements.txt
    startCommand: streamlit run gemini-code-1780037330332.py --server.port $PORT --server.address=0.0.0.0
    autoDeploy: true
    plan: free
